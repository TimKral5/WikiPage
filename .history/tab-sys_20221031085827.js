(() => {
    var tabs = {};

    document.querySelectorAll("div.tabgroup").forEach(tabgroup => {
        for (var tab in tabgroup.querySelectorAll("div.tab")) {
            tabs[tabgroup.]
        }
    });

})();